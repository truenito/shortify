<p id="notice"><%= notice %></p>


<h1><u>Shortify</u></h1>
<br>
<%= link_to 'Top 100 URLS JSON', top_path, class: 'pull-right btn btn-primary', style: 'margin: 5px 0; color: white;' %>
<br>

<table class='table table-striped '>
  <thead class='thead-dark'>
    <tr>
      <th>Title</th>
      <th>original link</th>
      <th>Shortened link</th>
      <th>Description</th>
      <th>Total Visits</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @urls.each do |url| %>
      <tr>
        <td><%= url.title %></td>
        <td><%= url.original_link %></td>
        <td><%= url.shortened_link %></td>
        <td><%= url.description %></td>
        <td><%= url.visit_count %></td>      
        <td><%= link_to 'Show', url %></td>
        <td><%= link_to 'Edit', edit_url_path(url) %></td>
        <td><%= link_to 'Destroy', url, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Url', new_url_path, class: 'pull-right btn btn-success', style: 'margin: 5px 0; color: white;' %>
